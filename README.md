# Yuki Backend

This backend provides auth, users, bookings, posts, lessons, and products APIs.

- Requires Node 18+.
- Environment configuration via `.env` as needed by the enabled routes.
- Static uploads served under `/files`.

See `app.js` and `routes/` for available endpoints.
